name: squareConfig
type: base
inherit: pbc_squareconf

fields:
  - name: url
    type: text

  - name: created
    type: autodate
    onCreate: true
    onUpdate: false

  - name: updated
    type: autodate
    onCreate: true
    onUpdate: true
